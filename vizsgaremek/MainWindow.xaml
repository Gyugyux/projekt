<Window x:Class="vizsgaremek.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:vizsgaremek"
        mc:Ignorable="d"
        Title="Adatbáziskezelő" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="SearchTextBox"
         Margin="0,0,0,10"
         Height="30"
         KeyUp="SearchTextBox_KeyUp"
         ToolTip="Írj be szöveget a kereséshez..."/>


        <DataGrid x:Name="CustomersDataGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  SelectionChanged="CustomersDataGrid_SelectionChanged"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="Auto" IsReadOnly="True"/>
                <DataGridTextColumn Header="Név" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <StackPanel Margin="10">
                <TextBlock Text="Név:"/>
                <TextBox x:Name="NameTextBox" Width="200"/>
            </StackPanel>

            <StackPanel Margin="10">
                <TextBlock Text="Email:"/>
                <TextBox x:Name="EmailTextBox" Width="200"/>
            </StackPanel>

            <Button Content="Hozzáadás" Click="AddCustomer_Click" Width="100" Margin="10"/>
            <Button Content="Módosítás" Click="UpdateCustomer_Click" Width="100" Margin="10"/>
            <Button Content="Törlés" Click="DeleteCustomer_Click" Width="100" Margin="10"/>
        </StackPanel>
    </Grid>
</Window>
